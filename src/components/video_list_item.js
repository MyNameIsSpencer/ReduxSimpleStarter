import React from 'react';


{/*  putting  video here  V   can replace the props.video line*/}
const VideoListItem = ({video}) => {
  {/*const video = props.video*/}
  return <li>Video</li>
};

export default VideoListItem;
